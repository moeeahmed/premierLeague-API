header.header 
    nav.nav.nav--links
        a.nav__el(href='/') Home
        a.nav__el(href='/fixtures') Fixtures
    nav.nav.nav--user
        if user
            a.nav__el(href='/account/settings') Account
            a.nav__el.nav__el--logout Logout
        else 
            a.nav__el.nav__el--login Log in
            a.nav__el.nav__el--cta Sign up
